<Project>

  <ItemGroup>
    <Compile Update="@(Compile)">
      <Visible Condition="'%(NuGetItemType)' == 'Compile' and '%(NuGetPackageId)' == 'DefaultApplication.Plugin.Messenger'">false</Visible>
    </Compile>
  </ItemGroup>

</Project>